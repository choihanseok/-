# ZC-TASK-20260909-004 — Session/Login Foundation

## Status
PASS

## Scope
Session/Login foundation only.

Implemented:
- SessionStatus
- AuthSession entity
- AuthSessionRepository contract
- InMemoryAuthSessionRepository
- SqliteAuthSessionRepository
- SessionService
- auth_sessions migration
- DB-backed integration tests
- reusable CI trigger for future zc-task branches

Not implemented:
- JWT format/signing
- production token transport policy
- cookie policy
- real SMS provider
- SNS login
- real-name verification
- Role/Permission
- UI

## Flow
VERIFIED LOGIN Challenge → Account lookup by phone → Session create → opaque access/refresh tokens → token hashes persisted.

## Security
- raw access/refresh tokens are not persisted
- HMAC-SHA256 token digests are stored
- refresh rotates both access and refresh tokens
- old refresh token becomes unusable after rotation
- session revoke is idempotent
- revoked sessions cannot authenticate
- access and refresh expiration are checked separately
- expired refresh marks session EXPIRED
- login session rejects a verified challenge with a non-login purpose

## PROJECT_DEFINED / NEED_REVIEW
Test foundation values:
- login purpose: LOGIN
- access TTL: 15 minutes default
- refresh TTL: 30 days default
- opaque random token: 32-byte base64url

Production values and JWT/opaque decision remain NEED_REVIEW.
Cookie/header transport, same-device policy, concurrent sessions, global logout, account-status login policy, refresh reuse detection, session limit, IP/device metadata and secret management remain NEED_REVIEW.

## Migration
`migrations/003_create_auth_sessions.sql`

Reference migration only. Production DBMS/ORM remain NEED_REVIEW.

## Impact
Account Entity/Service/Repository and AuthChallenge contracts are unchanged.
No Order, Payment, Settlement, Role/Permission or UI changes.

## Test Result
GitHub Actions `ZERO CALL Foundation Test` completed successfully.

Validated:
- DB-backed session creation from VERIFIED LOGIN challenge
- valid access-token authentication
- non-LOGIN challenge rejection
- refresh-token rotation and old-token invalidation
- idempotent revoke and revoked-session blocking
- separate access/refresh expiry enforcement
- all prior Account/Auth regression tests in the same `npm test` suite

## Safety
- deleted files: NONE
- deleted APIs/tables/data: NONE
- production DB access: NONE
- force reset/clean: NONE

## Pull Request
Draft PR #3: `ZC-TASK-20260909-004 Session/Login Foundation test`
Base: `zc-task-20260909-003-auth-foundation`
Head: `zc-task-20260909-004-session-login-foundation`

## Next Task
Do not automatically continue from this Task.
Recommended next task: `Role/Permission Foundation` after reviewing the existing 03_USER_ROLE_PERMISSION specification.
