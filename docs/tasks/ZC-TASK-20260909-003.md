# ZC-TASK-20260909-003 — Authentication Foundation

## Status
PASS

## Scope
Phone verification foundation only.

Implemented:
- AuthChallengeStatus
- AuthChallenge entity
- AuthChallengeRepository contract
- InMemoryAuthChallengeRepository
- SqliteAuthChallengeRepository
- PhoneVerificationService
- VerificationCodeSender contract
- auth_challenges migration
- unit/integration tests

Not implemented:
- real SMS/MMS provider
- login session/JWT
- refresh token
- SNS login
- real-name verification
- Role/Permission
- UI

## Impact
Existing Account Entity, AccountService, AccountRepository contract, Payment, Order, Settlement are unchanged.

## Security
- raw verification code is not persisted
- HMAC-SHA256 digest is stored
- constant-time hash comparison is used
- challenge TTL and attempt limit are enforced
- repeated verification of an already VERIFIED challenge is idempotent

## PROJECT_DEFINED / NEED_REVIEW
Test foundation values:
- code length: 6 digits
- TTL: 5 minutes
- max attempts: 5 default

Production values remain NEED_REVIEW.
SMS provider, resend/rate-limit policy, phone normalization, purpose master, session/JWT policy and secret management remain NEED_REVIEW.

## Migration
`migrations/002_create_auth_challenges.sql`

Reference migration only. Production DBMS remains NEED_REVIEW.

## Tests
GitHub Actions run `34342076111` on Node 22.23.2:
- total: 16
- pass: 16
- fail: 0
- Account Foundation regression: PASS
- SQLite Account Repository integration: PASS
- Authentication Foundation: PASS

Authentication coverage:
1. request challenge without persisting raw code
2. verify correct code
3. reject invalid code and count attempts
4. fail challenge after max attempts
5. expire challenge after TTL
6. repeated verification remains idempotent

## Safety
- deleted files: NONE
- deleted APIs/tables/data: NONE
- production DB access: NONE
- external SMS calls: NONE
- existing Account contract changes: NONE

## Follow-up
GitHub Actions reports a deprecation warning because `actions/checkout@v4` and `actions/setup-node@v4` target an older Node runtime internally. The workflow itself passes. Upgrade of workflow actions should be handled as a separate small maintenance task rather than mixed into this authentication task.

## Next Recommended Task
Role/Permission Foundation after authentication/session policy is defined, or Session Foundation once token/session policy is confirmed.
