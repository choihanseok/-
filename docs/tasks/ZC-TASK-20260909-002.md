# ZC-TASK-20260909-002 — Account DB Repository Adapter

## Status
PASS

## Scope
Account Foundation DB Repository connection only.

## Result
`AccountService → SqliteAccountRepository → accounts table` integration path is working in automated tests.

Implemented:
- `SqliteAccountRepository`
- actual SQLite-backed persistence for Account
- migration execution against SQLite test DB
- DB-backed create/get/status-change flow
- duplicate phone protection at service/repository/DB boundary
- AccountStatus CHECK constraint verification
- Node 22 CI runtime for `node:sqlite`
- GitHub Actions integration test

Not implemented:
- login
- JWT/session
- phone verification
- SNS login
- Role/Permission
- Order
- Payment
- Settlement
- UI
- production Framework selection
- production ORM selection
- production DBMS selection

## Impact
- Account Entity unchanged.
- AccountRepository contract unchanged.
- AccountService unchanged.
- Existing InMemoryAccountRepository retained for fast unit tests.
- Existing reference migration reused without destructive change.
- No existing API/table/data deletion.

## DB Adapter
Test adapter:
- SQLite using Node.js `node:sqlite`
- repository: `src/account/sqlite-account-repository.js`

The adapter implements the same AccountRepository contract used by InMemoryAccountRepository, so AccountService does not depend on a specific DB implementation.

## Migration
Reused:
- `migrations/001_create_accounts.sql`

The migration was executed against an actual SQLite in-memory database during integration tests.

Production DBMS remains NEED_REVIEW; therefore this does not declare SQLite as ZERO CALL production DB.

## Tests
Existing Account Foundation unit tests retained.

Added DB integration tests:
1. create/store/retrieve through SQLite repository
2. persist AccountStatus change
3. reject duplicate phone
4. reject invalid AccountStatus at DB CHECK constraint boundary

GitHub Actions:
- workflow: `Account Foundation Test`
- Node: 22
- result: SUCCESS
- run id: `34341327995`

## Changed Files
Added:
- `src/account/sqlite-account-repository.js`
- `test/account-sqlite-repository.test.js`
- `docs/tasks/ZC-TASK-20260909-002.md`

Updated:
- `package.json`
- `.github/workflows/account-foundation-test.yml`

Deleted:
- NONE

## Safety Result
- existing Account Entity preserved
- existing Repository contract preserved
- existing Service preserved
- InMemory Repository preserved
- no force reset/clean
- no production database access
- no table/data deletion
- no authentication/role/order/payment/settlement changes

## NEED_REVIEW
- final production Framework
- final ORM/data-access library
- final production DBMS
- production DB connection/pool strategy
- transaction strategy
- migration tool
- final PK strategy
- final AccountStatus master confirmation
- phone normalization policy
- production DB backup/restore policy

## Next Task
Do not automatically continue beyond this task.

Recommended next step when production stack is confirmed:
`Account DB Infrastructure — Production Connection / Migration Runner / Repository Adapter`

That task should adapt the same repository contract to the confirmed production DB without changing Account domain behavior unless a documented requirement requires it.
